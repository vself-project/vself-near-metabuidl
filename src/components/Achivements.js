import React from 'react';

export const Achivements = ({ wallet, account }) => {
  return <div style={styles.container}></div>;
};

const styles = {
  container: {
    width: 600,
    height: 140,
    backgroundColor: 'white',
    borderRadius: 4,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    padding: '10px 10px 10px 10px',
    border: '1px solid black',
  },
};
